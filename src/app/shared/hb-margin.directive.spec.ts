import { HbMarginDirective } from './hb-margin.directive';

describe('HbMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new HbMarginDirective();
    expect(directive).toBeTruthy();
  });
});
